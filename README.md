# BigHammer.ai LinkedIn sequences — iOS inbox preview

Static preview of every LinkedIn outreach message rendered as the prospect sees it in the LinkedIn iOS app.

Edit copy in `data.js`. No build step.
